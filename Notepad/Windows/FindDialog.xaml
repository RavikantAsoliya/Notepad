<Window x:Class="Notepad.Windows.FindDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Notepad.Windows"
        mc:Ignorable="d"
        Title="Find" Height="159" Width="370"
        Background="#F0F0F0" ResizeMode="NoResize" Loaded="Window_Loaded">
    <Grid Margin="10, 10, 10, 12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="75"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" Height="20">
                <TextBlock Text="Find what:" Height="20" Width="65"/>
                <TextBox Name="FindTextBox" Width="185" Height="20" TextChanged="FindTextBox_TextChanged" Margin="7,0,0,0"/>
            </StackPanel>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Bottom">
                    <CheckBox Name="MatchCaseCheckBox" Content="Match case" Margin="0,0,0,10"/>
                    <CheckBox Name="WrapAroundCheckBox" Content="Wrap around"/>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Top">
                    <GroupBox Header="Direction" HorizontalAlignment="Left"  VerticalAlignment="Top" Width="120" Height="48" Margin="0,10,0,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5,0,0">
                            <RadioButton Name="UpRadioButton" Content="Up" Margin="0,0,10,0"/>
                            <RadioButton Name="DownRadioButton" Content="Down" IsChecked="True"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </Grid>
        </Grid>
        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Top">
            <Button Name="FindNextButton" Content="Find Next" Margin="0,0,0,10" Click="FindNextButton_Click"/>
            <Button Name="CancelButton" Content="Cancel" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
